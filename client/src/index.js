import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';

// index.js will essentially find the div with id="root" and throw App into it
ReactDOM.render(<App />, document.getElementById('root'));
